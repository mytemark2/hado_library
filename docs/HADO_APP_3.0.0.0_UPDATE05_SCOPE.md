# HADO App 3.0.0.0 Update05 Scope

型候補一覧から部隊単位の候補トレイへ追加する。配置確定は直接実行せず、既存の部隊編成候補ダイアログへ委譲する。

- 保存項目: `evaluationTypeId`, `candidateTray`
- 接続対象: 主将、副将、補佐、侍従、装備
- 後続接続: 陣形、兵器、名馬、軍馬技能
- `assignFormationItem` を直接呼ばず、既存ゲートを迂回しない。
- 開発ブランチでは相対JS読込を使用し、正式配布時に単体HTMLへインライン化する。
