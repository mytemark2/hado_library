# 3.1.0.0 r203 検索結果パネル背景の是正

## 原因

r202は内部一覧`.results`を対象にしており、利用者が指定した親の検索結果パネル`.panel`を変更していなかった。

## 修正

`#results`を直接の子に持つ検索結果パネルだけをCSSで特定し、背景を`#eff6ff`に変更する。

## 非変更範囲

- 検索結果カード、一覧内部、詳細パネル、検索ロジック、JSONは変更しない。
