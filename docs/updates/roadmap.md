# 覇道ライブラリ 3.0.1.0 全体ロードマップ

## 目的
型検索を、単なる検索機能ではなく、部隊編成画面で完成形を検討するための候補提示機能へ段階的に拡張する。

## 基本方針
- 型検索は候補表示を担当し、組み合わせ検討と最終登録は部隊編成画面で行う。
- 主将、副将、補佐、侍従、装備、陣形、兵器、名馬、軍馬技能の9役割を対象とする。
- 既存の部隊編成成立ゲートを迂回しない。直接配置は行わず、既存選択処理へ委譲する。
- 既存保存データを上書きしない。型検索から作成する部隊は新規部隊として追加する。
- 全データ表示と保存データ表示を分離し、保存データ表示では所有情報と設定値を反映して候補を絞り込む。
- HTMLを肥大化させない。新規責務は外部JSへ分離し、HTMLは読込宣言と最小限の初期化に留める。
- JSファイル名はUpdate番号ではなく責務名を使用する。`updateNN_xxx.js`、`xxx_updateNN.js`、`temp_xxx.js`、`new_xxx.js` のような一時命名は禁止する。
- 既存責務へ統合できる場合は、責務一致する既存JSへ追記する。新規JS作成時は既存ファイルへ統合できない理由を記録する。
- アプリ開発ブランチへのPush後はプレビューへ自動同期する。

## 10回のUpdate計画

### Update01: 3.0.0.0開発基盤とプレビュー導線
**状態:** 完了

- `future/app-3.0.0.0` を開発中正本とする。
- GitHub運用ルール、プレビュー確認導線、外部JS分離の基盤を整える。
- `main` は正式公開済みソースとして直接Pushしない。

### Update02: クローラーJSON取込監査と派生JSON統合
**状態:** 完了

- クローラー生成JSONを正本として安全に取り込む。
- JSON構文、必須ファイル、派生索引、互換性を監査する。
- 型検索用の派生JSONとvaccine型上書きを統合する。
- アプリ側補完は旧JSONフォールバックと安全ゲートに限定する。

### Update03: 型編成ナビのウィザード化
**状態:** 改訂完了

- 型検索の入口をウィザード形式へ変更する。
- 主将から考える導線を採用する。
- 目的を次の7用途へ整理する。
  - 対部隊殲滅
  - 城壁破壊
  - 城壁防衛
  - 要所攻防
  - 駐屯
  - バフ支援
  - デバフ妨害
- 各目的に主軸型、補助型、理由表示を持たせる。

### Update04: 9役割の型候補一覧
**状態:** 改訂完了

- 主将、副将、補佐、侍従、装備、陣形、兵器、名馬、軍馬技能の候補一覧を表示する。
- 参考一致数が0件の候補を除外する。
- 候補閲覧と配置処理を分離する。
- 既存の成立ゲートを迂回しない。

### Update05: 部隊別候補トレイと既存ダイアログ委譲
**状態:** 完了

- 部隊単位で `evaluationTypeId` と `candidateTray` を保存する。
- 主将、副将、補佐、侍従、装備を候補トレイから既存選択ダイアログへ渡す。
- `assignFormationItem` を直接呼ばず、既存ゲートを必ず通す。

### Update06: 残り4役割の候補トレイ接続
**状態:** 完了

- 陣形、兵器、名馬、軍馬技能を候補トレイから既存選択処理へ接続した。
- 9役割すべてで、候補一覧 → 候補トレイ → 既存選択処理の経路を統一した。
- 陣形は既存陣形マスタ選択と侍従3×3修復処理へ委譲する。
- 兵器は既存兵器マスタ選択とLv正規化処理へ委譲する。
- 名馬は所有済み軍馬を既存の軍馬3枠割当処理へ委譲する。
- 軍馬技能は対象技能を持つ所有済み軍馬を探索し、既存の軍馬3枠割当処理へ委譲する。
- 主将、副将、補佐、侍従、装備では既存選択ダイアログを維持し、兵科、射程、侍従3×3条件、装備重複の成立ゲートを迂回しない。
- 候補トレイUIは `hado_candidate_tray.js`、保存・配置・成立判定は `hado_formation.js` に統合し、重複イベント処理を持つ `hado_candidate_tray_core.js` は読み込まない。Update番号付きJSファイルは残さない。

### Update07: 適合スコア表示と評価ロジック調整
**状態:** 完了

- 型ごとの採点項目は5項目、重みなしとする。
- 技能Lvは採点しない。
- 兵力は採点対象に含める。
- 型充足度という別指標は設けない。
- 適合スコアは10段階で理解しやすく表示する。
- 殲滅系のスコアが全体的に高くなりすぎないよう補正する。
- 主軸理由と補助理由を表示する。
- Update08で所有情報フィルター適用後にも同一ロジックを再利用できるようにする。

### Update08: 保存データ対応型編成ナビ、新規部隊作成、保存評価、履歴、グループ管理
**状態:** 完了（最新: Update08.23）

- 保存データ表示モードで型編成ナビを利用できるようにする。
- 全データ表示では全武将、全装備、全名馬などを候補対象とする。
- 保存データ表示では、所有登録済みの武将、装備、名馬などに候補を限定する。
- 保存データに登録された将星、技能Lv、装備所有状況などの設定値を候補表示と評価へ反映する。
- 型検索から新規部隊を作成する際、選択中の型を自動入力する。
- 既存部隊を上書きせず、新規部隊として追加する。
- 編成結果に評価型ID、トータルスコア、評価スコア、マイメモを保持する。
- 1部隊あたり最大10件の履歴を保持する。
- 1グループあたり最大12部隊、最大5グループを管理する。

### Update09: UI/UX調整と実用性改善
**状態:** 完了（3.0.0.0 Update09.5.65 / r155、公開Preview確認済み）

- 主将候補は新武将順で確認できるようにする。
- 武将変更はダイアログで行えるようにする。
- 候補ワークスペースの編集/候補モードで、確認、複数選択、配置、解除を一つの画面に統合する。
- 全データ表示と保存データ表示の違いを画面上で明確にする。
- PC表示とスマホ表示を調整し、はみ出し、操作困難、過剰な再描画を解消する。
- 型検索は候補表示に留め、組み合わせ検討は部隊編成画面で行う設計を維持する。

### Update10: 全体回帰、ガイド更新、正式版候補
**状態:** 完了（Update10.1〜Update10.4 / r159、全体回帰・公開Preview・正式版main反映確認済み）

- Update10.1では、共通タブ基盤、上位画面、検索モード、内容詳細、部隊編成内、候補ワークスペース、型編成ナビの選択状態と切替フィードバックを改善する。
- Update10.1の第1〜3段階は完了。タブ自身の選択強調を維持し、表示領域を圧迫していた「〇〇を表示中」補助行と上位タブの「表示中」バッジは撤去した。
- Update10.2では、スタート・検索・部隊編成・軍馬編成ガイドを、検索モード独立、型編成ナビ3入口、候補ワークスペース復元、6項目結果サマリーの現行導線へ同期した。
- Update10.3では、ガイドの文字間隔・行間、初回ガイド開始時と検索ガイド8番目の通常検索同期、部隊編成ガイド8番目の表示中結果サマリー選択を改修した。
- Update10.4では、クローラー`1.1.0.2`の最新JSON一式と、手動保守する状態変化6件を反映した。
- 通常検索、状態変化検索、型検索、詳細、部隊編成、Import / Export、保存互換を回帰確認済み。
- 全データ・保存データ、9役割候補、候補ワークスペース、既存成立ゲート、保存履歴、グループ管理を確認済み。
- PC、スマホ、`file://`、`https://`、イベント駆動Preview同期、操作ガイドを確認済み。
- 配布用ZIPは当面公開しない。正式版はPR #259で`main`へ反映済みで、画面には`3.0.0.0`のみ表示する。
- Update09の完了版 `3.0.0.0 Update09.5.65 r155` を回帰の起点とし、個別の改修要求はUpdate10の優先度と受入条件を定義してから開始する。

### Update11: タグ整理と状態変化検索タグ対応
**状態:** 実装・検証中（3.0.1.0 Update11 / r160）

- タググループへ対象カテゴリを表示する。
- タググループを検索カテゴリの表示順で並べる。
- 状態変化検索とタグ入口を同一行にし、状態変化条件とタグ条件をANDで適用する。
- 状態変化検索専用の同期・非同期・キャッシュ・履歴経路で同じタグ条件を利用する。

## Update記録ルール
- 各Updateは `docs/updates/updateNN/roadmap.md`、`implementation.md`、`report.md` に記録する。
- 枝番修正は `Update05.1` のように管理する。
- 実装時は本ロードマップとの差分を `implementation.md` に記載する。
- 完了時は確認済み、未確認、未解決、次Update候補、プレビュー反映結果を `report.md` に記載する。
- 新規JS作成時は責務名を使用し、Update番号、一時作業名、枝番をファイル名に含めない。

## 2026-06-27 Update09.5.1

- Phase 5 starts from completed Phase 4 (`Update09.4.21` / revision `94`) and focuses on type search/candidate/tray speed and handoff fixes.
- Implemented lazy type-candidate role counts, explicit candidate-tray add flow, current-formation candidateTray persistence/deduplication, and type-search unmeasured-time diagnostics.
- Preview verification remains pending until the committed branch can be pushed from an environment with configured remotes.

## 2026-06-27 Update09.5.2

- Added direct current-formation type selection to the total score panel and immediate recalculation/persistence for `evaluationTypeId` / `evaluationTypeName`.


- Update09.5.4: 部隊編成の評価スコア詳細タグで、根拠名の直後に発生元を括弧付き表示する回帰を修正。


- Update09.5.5: 評価スコア詳細で未定義 `sr-only` に依存した根拠集約テキストが露出する回帰を修正し、再発防止 validator を追加。


- Update09.5.6: 評価スコア内訳を通常時1行、さらに表示時は結果サマリー風グリッドにし、欄外表示を防止。


- Update09.5.7: 評価スコア内訳の `さらに表示` を結果サマリーと同じ別ダイアログ表示にし、根拠を全件表示。

- Update09.5.8: 評価スコア chip クリックで詳細が切り替わらない duplicated score card binding 不具合を修正。

- Update09.5.9: 評価スコア判定を targetScope aware に変更し、非ダメージ/自部隊不利対策/味方回復の対象判定と二重加点防止を追加。
- Update09.5.10: 評価スコア旧ランタイムパッチを撤去し、メタ同期ファイルからスコア計算・描画上書きを再導入できない検証を追加。
- Update09.5.11: 可視バージョンの固定記載を runtime から撤去し、`hado_version.js` 単一ソースと validator で再発防止。
- Update09.5.12: 評価スコア内訳パネルから「さらに表示」を撤去し、クリックで詳細ダイアログを開く方式へ変更。
- Update09.5.13: 評価スコア詳細ダイアログ内の根拠タグを省略せず全文表示するように変更。
- Update09.5.14: 評価スコア根拠判定をカテゴリゲート化し、自部隊不利対策への防御・回復・火力系混入と同一根拠二重加点を防止。

## 2026-06-28 Update09.5.15
- 評価スコア根拠を一次効果に限定し、型要素・派生タグ・変化率集計・対象不明の効果が味方非ダメージ効果や他の対象依存カテゴリへ混入しないよう targetScope / effectKind / category gate / evidence origin / dedupe を再監査。

## 2026-07-11 JSONインデックス契約修正（表示バージョン据え置き）

- 現在の正本 `3.0.0.0 Update09.5.48 / r138`。オブジェクト形式テーブルの他武将一覧除外修正を識別するため表示版を更新した。
- クローラーの派生生成契約を修正し、20個の派生JSONを同じ入力から一括再生成する。アプリ側でJSONを手修正しない。
- P0/P1対象として、型検索feature/role、関連リンク、技能所有者、parameter summary、状態変化、戦法攻撃、検索・表示用索引を同時に監査する。
- アプリは `sourceEntityKey`、`sourcePartType`、`evidencePath`、`canonicalFeatureKey`、`featureDomain`、`roleGate` を使い、集計値・記事本文・補佐/侍従の戦法を型評価へ混入させない。
- 計画書の8必須回帰ケースと、全根拠本文が指定された一次ソース位置に実在することをリリースゲートにする。
- ローカル検証後にアプリPR、クローラー既存PR、GitHub Actions、イベント駆動preview同期、公開Pagesの順で確認する。

## 2026-07-15 Update09.5.56

- 武将検索から兵科基本能力・兵科ランク・解説を除外し、候補トレイ削除と型候補選択を同期する。
- 型編成ナビ/型候補一覧の大容量JSONを共有し、採点・DOMを段階処理する。全データ/保存データ切替は維持済み索引を再利用する。
- ローカル112項目検証と390x844実操作後、PR、Actions、イベント駆動Preview、公開markerと実操作を完了条件とする。

## 2026-07-19 Update09.5.64

- 候補ワークスペースの編集操作を即時反映へ変更し、新規部隊作成を候補モードへ移す。
- 型編成ナビで選択した主将と編集済み候補を作成前draftへ保持し、新規部隊へ型・候補・主将を一括で引き継ぐ。既存部隊の候補は変更しない。
- 表示版を `3.0.0.0 Update09.5.64 r154` とし、全アプリ検証、Actions、公開marker、PC/スマホ実操作を完了条件とする。

## 2026-07-19 Update09.5.65

- 型編成ナビの作成前候補を閉じた後、通常ボタン、イベント、ページ再読込のどの経路でも同じdraftとして再開する。
- 候補、選択主将、編集・候補モード、役割タブ、絞り込み、表示件数を一体で保存し、既存部隊の候補とは混在させない。
- 候補ワークスペースの件数表示も未完了draftを優先し、部隊作成完了後だけ既存部隊の候補へ戻す。
- 表示版を `3.0.0.0 Update09.5.65 r155` とし、再開経路の実行回帰、全アプリ検証、Actions、公開Previewを完了条件とする。
