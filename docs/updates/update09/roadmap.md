# Update09 Roadmap

## 目的
Update08までに実装した型編成ナビ、候補トレイ、保存データ連携、部隊編成評価を、実戦で迷わず・軽く・見やすく使えるUI/UXへ整理する。

## 現在の状態
- 状態: Phase 5 最終監査中（Phase 4はUpdate09.4.21で完了、実装中の正本: 3.0.0.0 Update09.5.59 / r149）
- 調査開始HEAD: `779c15c5193949a2122004f2cbbf297f969a8ca9`
- 対象ブランチ: `work`
- Update08最新状態: `Update08.23`（型候補描画キャッシュと重い診断の遅延化まで完了）
- Update09表示ルール: Phaseが分かるように `Update09.x.y` で表示する（`x` はフェーズ番号、`y` は当該フェーズ内の改修回数）。

## 2026-07-11 JSONインデックス契約修正（表示バージョン据え置き）

- 修正ベース: `feature/app-3.0.0.0` / `49a71accb3228c7a8a82b3988150186d15dd19e1`。
- `hado_version.js` を `3.0.0.0 Update09.5.48 / r138` へ更新し、`HADO_DEV_INFO.json` の更新日時も同期する。
- クローラー生成元の修正、20派生JSONの一括再生成、アプリ消費側の契約ゲート、8必須回帰ケースを一つの変更として扱う。
- 完了ゲートは、全JSON/JS/HTML構造、検索・詳細・型候補・部隊評価・保存Import/Export、PC/スマホ、PR検証、preview marker、公開Pagesの一致確認とする。
- `.5.16` から `.5.41` の履歴は本作業で推測補完しない。リモート検証未完の間はpreview完了扱いにしない。

## 優先テーマ
1. 部隊編成画面と型編成ナビ/型候補一覧の体感性能改善。
2. 部隊編成のレイアウト整理と、武将・装備変更のダイアログ/ポップアップ化の完成。
3. ガイドの最新化と、必要な時だけ読める段階的な説明表示。
4. 補足説明の常時表示を減らし、画面密度を下げる。
5. 型編成ナビ → 型候補一覧 → 候補トレイ → 部隊編成の操作導線を直感化する。
6. PC/スマホそれぞれの表示破綻、はみ出し、操作困難箇所を回帰確認する。

## 完了条件
- 課題ごとにユーザー受け入れ操作を定義し、実装後にPC/スマホ双方で確認する。
- HTMLへ大型ロジックを追加せず、既存責務の外部JS/CSSへ統合する。
- 型検索は候補提示、組み合わせ検討は部隊編成画面という設計を維持する。
- 既存の成立ゲート、保存データ互換、Export/Import互換を維持する。
- Update09の実装内容、HTMLサイズ増減、外部化判断、検証結果、プレビュー同期結果を記録する。

## フェーズ計画

### Phase 1: UI/UX監査と改善案整理（本PR）
- 現行実装とUpdate08.23の残課題を確認する。
- 問題点を分類し、各項目に3案ずつ改善案を作成する。
- 実装順序の推奨を決める。

### Phase 2: 性能・再描画改善
- 部隊編成画面の全体再描画を、操作種類別に必要範囲へ縮小する。
- 型候補一覧と型編成ナビの初回表示・検索・タブ切替・トレイ追加時の再計算を抑制する。
- PC版でのスクロール、固定サイドバー、選択ダイアログ表示時の体感停止を計測・改善する。

### Phase 3: 部隊編成レイアウトと変更ダイアログ完成
- 完了: 2026-06-23。Previewでの表示確認とユーザー受け入れが完了し、Phase 3残課題はなし。
- 武将/装備変更をPCでもモーダル/ポップアップ中心に寄せる。
- 編成盤面、選択中スロット、結果サマリー、部隊一覧の優先順位を整理する。
- 候補トレイからの配置先選択を既存選択ダイアログへ自然につなげる。
- 部隊編成のグループは、グループ名と主要ボタンを含めて1行で表示する。グループ名変更は別ダイアログで行い、一覧行を肥大化させない。
- 型選択は重複表示を再考し、同じ選択状態や説明が複数箇所へ出ないようにする。表示型IDはユーザー向け通常表示から外す。
- 部隊のトータルスコアや評価スコアは自動計算結果として扱い、入力ダイアログにはしない。各表示スコアはトータルスコアの下に従属表示する構成へ再考する。
- 履歴へ保存ボタンは不要とし、保存ボタン押下時に現行内容を保存する。マイメモは1行表示に絞り、編集時だけ別ダイアログで編集する。

### Phase 4: ガイド・補足説明・導線整理
- 着手: 2026-06-25。Update09.4.1としてスタートガイドとタブ別ガイドの導線文言を先行更新した。
- 進行中: 2026-06-26。Update09.4.2として型候補一覧と候補トレイに短い「次の操作」ガイドを追加した。
- 追加対応: 2026-06-26。Update09.4.3として部隊スコア表示の合計値スコープを堅牢化し、類似の未定義参照をセルフチェックへ追加した。
- 進行中: 2026-06-26。Update09.4.4として部隊編成画面へ折りたたみ式の「次の操作」ヘルプを追加し、グループ切替・変更・配置・保存の順序を画面内で確認できるようにした。
- 追加対応: 2026-06-26。Update09.4.5として部隊グループの現在名を表示し、「変更」ボタンを重複IDに依存しないイベント配線へ修正した。
- 追加対応: 2026-06-26。Update09.4.7として部隊グループ選択の旧Updateメタ上書きを撤去し、1行のリストボックス+変更ボタン表示を実際の表示にも反映する。
- スタートガイド、型編成ナビ、型候補一覧、部隊編成内の文言をUpdate09時点へ最新化する。
- 長い補足は `details`、ヘルプアイコン、モーダルに移し、常時表示は短い行動ラベルへ絞る。
- 全データ表示/保存データ表示の差と、次に押すべきボタンを画面上で明確にする。

### Phase 5: 回帰・プレビュー確認
- JavaScript/JSON/HTML/CSS/ワークフロー検証を実行する。
- ツール全体の動作速度について、起動、検索、型編成ナビ、型候補一覧、候補トレイ、部隊編成、保存データImport/Exportのボトルネックを計測・特定し、必要な改善を実装して再計測する。
- PC/スマホの主要導線、保存データImport/Export、型候補からの新規部隊、候補トレイ配置を確認する。
- Push後のpreview同期と公開commit一致を確認する。

- 追加対応: 2026-06-26。Update09.4.8として部隊グループ直下の未依頼「次の操作」ヘルプを撤去し、グループ選択はリストボックス+変更ボタンだけに戻す。
- 追加対応: 2026-06-26。Update09.4.9としてPC表示の左側グループ/部隊一覧パネルに縦スクロールバーを明示し、固定パネル内で下部操作や部隊リストへ到達できるようにする。
- 追加対応: 2026-06-26。Update09.4.10としてPC左パネル全体ではなく部隊一覧リスト部分だけをスクロールさせ、グループ選択と操作ボタンの位置を固定する。
- 追加対応: 2026-06-26。Update09.4.11としてPC左パネル本体のスクロール位置を描画直後に0へ戻し、部隊一覧見出し・グループ選択・操作ボタンが初期表示で隠れないようにする。
- 追加対応: 2026-06-27。Update09.4.12としてPC左パネルのスクロール位置を即時・RAF・短時間後にリセットし、ブラウザ/再描画後の保持スクロールで上部操作が隠れないようにする。

- Update09.4.13: PC左パネル自体を `overflow:clip` にしてスクロールコンテナ化を止め、部隊カード一覧だけがスクロールするよう固定。即時/RAF/80ms/250ms/600ms のリセットと `scrollTo` を併用。

- Update09.4.14: preview/browser cacheで旧 `hado_styles.css` / `hado_formation.js` が残る可能性を潰すため、該当アセットの読み込みURLに `?v=09.4.14` を付与。

- Update09.4.15: `--mobile-fixed-stack-space` が初期計測0pxで小さく設定され、PC左パネルがヘッダー背面に潜る可能性を修正。下限118pxと計測ログを追加。

- Update09.4.16: 左パネルの見出し・グループ選択・操作ボタンを `formation-list-fixed-head` に分離し、部隊カード一覧だけをスクロール領域に固定。

## 2026-06-27 Update09.5.1 Phase 5

- Scope: type search profiling, type candidate role-count performance, type entry/candidate/tray handoff, and current formation candidateTray persistence.
- Status: implementation and local validation added; preview synchronization is blocked in this workspace because no git remote is configured.
- Follow-up: collect browser `typeSearch:profile` logs after deployment and compare `measuredKnownMs`/`unmeasuredMs` against the previous `totalMs=269.8ms` observation.

## 2026-06-27 Update09.5.2

- Formation score panel now owns direct type selection for the current formation; preview verification remains pending until remote access is available.


- Update09.5.4: 部隊編成の評価スコア詳細タグで、根拠名の直後に発生元を括弧付き表示する回帰を修正。


- Update09.5.5: 評価スコア詳細で未定義 `sr-only` に依存した根拠集約テキストが露出する回帰を修正し、再発防止 validator を追加。


- Update09.5.6: 評価スコア内訳を通常時1行、さらに表示時は結果サマリー風グリッドにし、欄外表示を防止。


- Update09.5.7: 評価スコア内訳の `さらに表示` を結果サマリーと同じ別ダイアログ表示にし、根拠を全件表示。

- Update09.5.8: 評価スコア chip クリックで詳細が切り替わらない duplicated score card binding 不具合を修正。

- Update09.5.9: 評価スコア判定を targetScope aware に変更し、非ダメージ/自部隊不利対策/味方回復の対象判定と二重加点防止を追加。
- Update09.5.10: `hado_update_meta.js` に残っていた旧評価スコア hotfix を撤去し、targetScope 判定が旧キーワード一致で上書きされないよう validator / Node テストを追加。
- Update09.5.11: `hado_version.js` 以外の runtime 固定 Update09.5.x 文字列を撤去し、index / trace / validator を単一バージョンソース運用へ戻した。
- Update09.5.12: 評価スコア内訳の「さらに表示」を廃止し、内訳パネルクリックで詳細ダイアログを開く UI に変更。簡略タグは重複排除。
- Update09.5.13: 評価スコア詳細ダイアログ内の根拠タグを省略せず全文表示するように変更。

### Update09.5.14 必須残対応 — 評価スコア判定カテゴリゲート監査

- 評価スコアで、対象判定だけに依存した誤加点が発生している。
- 自部隊不利対策に、防御・負傷兵回復など別カテゴリの効果が混入している。
- これは表示だけではなく、評価スコアの根拠抽出ロジックの問題である。
- 対象、効果種別、評価カテゴリ、除外条件、重複排除をセットで判定する必要がある。
- 類似カテゴリにも波及している可能性があるため、評価スコア全体を監査する。
- validator / Node testで再発防止する。

### Update09.5.15 必須残対応 — 評価スコア一次効果ゲート監査
- 評価スコアで、一次効果・派生タグ・状態変化タグ・型要素・部隊パラメータ集計・変化率集計・静的能力補正・対象不明の効果が混同され、対象判定だけに依存した誤加点が発生していた。
- 味方非ダメージ効果に `型要素 知力上昇`、`部隊の知力(変化率集計)`、対象不明の状態変化タグが根拠として混入していたため、表示ではなく評価スコアの根拠抽出ロジックを修正する。
- 自部隊不利対策に防御・回復など別カテゴリの効果が混入していた件と同じ欠陥クラスとして、対象、効果種別、評価カテゴリ、除外条件、一次効果 eligibility、重複排除をセットで判定する。
- 類似カテゴリにも波及している可能性があるため、評価スコア全体で集計値・派生タグ・対象不明を直接根拠にしない監査を行い、validator / Node testで再発防止する。

### Update09.5.48 Preview Pages監視経路

- `deploy-preview.yml` へPages公開を一本化し、アプリ側の完了待機と公開marker確認を同じ経路へ揃える。
- 完了条件は、両リポジトリのworkflow検証、Preview側Pages成功、公開commit/branch/version marker一致とする。

### Update09.5.49 武将検索表出典保持

- runtime正規化後も表の出典indexを保持し、他武将一覧を本人の検索文へ混入させない。
- 完了条件は、LR夏侯淵の実JSON回帰、全アプリ検証、公開Previewでの `関羽` 実検索、Actions/marker一致とする。

### Update09.5.50 公開runtime cache整合

- `hado_version.js` と検索runtimeへ同一update/revision cache keyを設定する。
- 完了条件は、公開見出し `Update09.5.50 r140`、一時バナー自動非表示、LR夏侯淵除外の実ブラウザ確認とする。

### Update09.5.51 他武将一覧横断除外

- 相性一覧・五行一覧・技能所有者一覧を本人の全文検索文から除外する。
- 完了条件は、LR夏侯淵・UR花鬘の実fixture、106項目検証、公開 `関羽` 検索とPreviewバナー非表示の再確認とする。

### Update09.5.52 Debug Log表示回帰

- `ログ表示` ON時に生成済みDebug Logパネルから初期非表示クラスを解除する。
- 完了条件は、専用runtimeテスト、107項目の全アプリ検証、公開Previewでのパネル表示・ログ生成・警告/エラーなしの確認とする。
- 状態: 完了。PR #221、両リポジトリActions、公開marker、PC/スマホ実操作を確認済み。

### Update09.5.53 武将全文検索の一次情報境界

- 武将検索の対象を、本人名・基本情報・戦法・技能などの実データへ限定する。
- 基本情報と戦法の間にある攻略評価・おすすめ・比較コメント、および列伝・演義・正史は検索対象外にする。
- 見出し名の個別列挙ではなくページ構造で攻略評価領域を識別し、将来見出しが追加されても混入しないようにする。
- 完了条件は、実データ481武将で `関羽` が6件だけになる専用回帰、全アプリ検証、Actions、marker一致、公開PreviewのPC/スマホ実検索確認とする。
- 状態: 完了。PR #223、108項目検証、両リポジトリActions、公開marker、PC/スマホ実検索、Debug Logを確認済み。

### Update09.5.54 IME確定後検索

- 日本語IMEの変換中は検索結果を更新せず、変換確定後に一度だけ検索する。
- 変換開始時に既存の検索予約を取り消し、IME確定用Enterを検索履歴へ登録しない。
- 完了条件は、IMEイベント回帰、全アプリ検証、Actions、公開marker一致、公開Previewでの版数・通常検索確認とする。
- 状態: 完了。PR #225、109項目検証、両リポジトリActions、公開marker、配備runtime、PC/スマホ実検索、Debug Logを確認済み。

### Update09.5.55 型候補一覧の侍従成立ゲート

- 型候補一覧の侍従から、ゲーム上侍従にできないLR武将を除外する。
- 型候補一覧、候補トレイ、直接追加、最終配置を同じ役割候補ポリシーへ統合し、候補表示と成立判定の不一致をなくす。
- 候補トレイの重複イベント・独自配置先生成を廃止し、候補に付与された役割以外の配置先を表示しない。
- 完了条件は、LR除外・UR維持・他役割でのLR維持の専用回帰、全アプリ検証、PC/スマホ、Actions、公開marker一致、公開Preview実操作とする。
- 状態: 完了。PR #227、110項目検証、両リポジトリActions、公開marker、配備runtime、PC/スマホ実操作、候補トレイ役割維持、Debug Logを確認済み。

### Update09.5.56 検索境界・候補同期・スマホ応答改善

- 武将全文検索から「兵科の基本能力」「兵科ランク上昇タイミング」「解説」と、同じ内容を持つ武将別兵科親セクション・兵科表を除外する。戦法・技能に実在する兵力効果は維持する。
- 候補トレイから行を削除または全削除した時、型候補一覧で対応する選択状態もsnapshotイベントから解除する。
- 型編成ナビと型候補一覧が39MB超の役割索引を別々に取得しない共通データストアを導入し、ナビの主将DOMと候補一覧DOMを段階表示する。候補採点は描画フレームへ処理を返しながら行う。
- 全データ/保存データ切替では維持済み保存索引を再利用し、部隊編成の再描画を次フレームへ分離する。
- 完了条件は、専用回帰、全アプリ検証、PC/390x844実操作、Actions、公開marker一致、公開Previewでの検索・選択同期・応答確認とする。
- 状態: 実装・ローカル112項目検証・390x844実操作完了。PR #229を作成しApp Validation成功。正本マージ、Preview Actions、公開実操作確認待ち。

### Update09.5.57 型候補複数選択・データ切替進捗・診断版数表示

- 型候補一覧では役割タブをまたいで複数の候補を選べ、選択済み件数を確認して一括で候補トレイへ追加できるようにする。
- 主将・副将・補佐など役割タブの件数は、PC/スマホとも省略記号にせず常に数字を表示する。スマホではタブ列を横スクロール可能にする。
- 全データ/保存データ切替では、実処理前に切替中overlayを表示し、検索結果・詳細・編成を更新中であることを示す。
- `？` 診断画面はビルドの基本版ではなく、画面表示と同じ `覇道ライブラリ 3.0.0.0 Update09.5.57 r147` を表示する。
- 完了条件は、複数選択とトレイ削除同期、役割タブ件数の非省略、切替中表示、診断版数の専用回帰、全アプリ検証、Actions、公開Preview確認とする。
- 状態: PR #230で正本へマージし、Actions/Preview配備成功。公開確認で役割件数の未集計表示を検出したため、Update09.5.58で恒久修正する。

### Update09.5.58 型候補の全役割件数確定表示

- 公開Preview確認で、副将以降の役割件数がCSS省略ではなく未集計プレースホルダー `…` のまま残ることを検出した。
- 型候補一覧は全役割の採点・件数集計を進捗表示付きで完了してから一覧を描画し、PC/スマホとも全タブを数値で表示する。
- 完了条件は、専用回帰、全アプリ検証、Actions、公開PreviewのPC/スマホで全9役割が数値表示されることとする。
- 状態: 完了。PR #231、112項目検証、両リポジトリActions、公開marker、PC/390x844実操作、全9役割の数値表示、複数選択、診断版数、Debug Logを確認済み。

### Update09.5.59 符号付き数値検索境界・保存データ往復監査

- `兵力+` のように末尾へ符号を指定した検索は、単に `兵力` を含むだけの武将を返さず、検索対象内に実在する `+数値` または `+数値%` の効果を持つ項目だけを返す。
- 半角・全角の正負記号、数値・単位付き指定を同じ汎用メトリック検索境界で判定し、兵力だけの個別修正にしない。
- 保存データのExport→Importについて、既存保存を維持した追加取込、同一ID上書き、編成・検索履歴の統合、永続化を実行回帰へ追加する。
- 完了条件は、114項目の全アプリ検証、Actions、公開marker一致、公開Previewで `兵力+` 106件・LR呂布0件、`兵力` 243件、`関羽` 6件を確認することとする。
- 状態: 実装・114項目の全検証・PC実検索完了。PR、Actions、公開Preview確認待ち。
