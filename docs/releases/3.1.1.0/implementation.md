# 3.1.1.0 実装記録

## 実装内容

- `hado_share.js`に8つのコピー・共有機能を集約した。
- `hado_share.css`に共有操作列と受信確認画面の表示を分離した。
- 一覧コピーは検索条件を先頭に置き、各行へカテゴリを付加した。数値検索時だけ該当値を併記する。
- 部隊共有文は、武将ごとに侍従名・侍従位置・武器・防具・文物を1行で出力する。
- 軍馬共有文は、通常技能を最大3件、各技能レベル付きで出力する。
- 詳細リンクはカテゴリと正規IDを使い、IDがないデータは名称を代替キーにする。
- 共有リンクは圧縮可能な環境ではdeflate-rawを使い、未対応環境ではBase64URLへ安全にフォールバックする。
- 部隊・軍馬の受信は追加専用とし、既存データを変更しない。
- 起動データの読込完了前に受信確認画面を出さないよう待機条件を設けた。

## 変更ファイルと責務

- `hado_share.js`: コピー文、共有URL、復元、追加専用インポート
- `hado_share.css`: 共有ボタンと確認画面
- `index.html`: 外部資産の読込だけを追加
- `hado_core.js`: 要素参照、表示入口、ビルド情報
- `hado_status_effects.js`: 一覧コピーを新仕様へ委譲
- `hado_bootstrap.js`: 起動時自己検査を新しいボタン名へ更新
- `hado_version.js` / `HADO_DEV_INFO.json`: `3.1.1.0 r204` Preview識別
- Preview workflow・検証スクリプト・回帰テスト: 新しいブランチと資産を検証対象へ追加

## r205 PC部隊編成レイアウト修正

- 分類: 3.1.1.0で追加したコピー操作列によるPCレイアウト回帰。
- 原因: コピー操作列を`.formation-detail-panel`直下へ独立行として追加し、画面高固定の編集領域を下へ押し下げていた。
- 修正: コピー操作を既存の`.formation-work-tabs`末尾へ移動し、PCでは「部隊編成／編成／戦法／変化率／条件／詳細」と同じ行の右端へ配置した。
- スマートフォン: 390×844では操作性を保つためメニュー直下に折り返し、横方向のはみ出しを発生させない。
- 再発防止: 契約テストで`panel.prepend(host)`を禁止し、`tabs.append(host)`と右寄せCSSを必須化した。
- 実ブラウザ確認: 1917×964でメニュー高41px、編集領域上端213px、画面横はみ出しなし。390×844でも両ボタン表示、横はみ出しなし。
- HTML: DOM構造の追加は行わず、既存外部ファイル`hado_share.js`、`hado_share.css`、`hado_styles.css`を修正した。`index.html`はキャッシュキーのみ更新した。
- Preview識別: `3.1.1.0 r205`。

## 2026-09-06 データ更新追従の検証修正

- 分類: 検証コードの古い実データ件数固定による偽陽性。
- 原因: `test_3_1_0_0_tag_search_exhaustive.js`が交戦開始時タグの武将件数を105件に固定していた。新しい取得データではLR李牧の俊英Lv2の説明更新により、元データにも索引にも106件が存在する。
- 修正: 非空の元データを必須とし、元の明示的な発動マーカーと検索対象の全武将名を完全一致で照合する。105など取得日依存の固定件数は使わない。
- 同種調査: 回帰テスト中の3桁以上の固定size/length等号判定と旧収集件数を調査。復元データテストの件数は最小保持件数であり、正当な追加を許容するため維持。
- 旧データ105件、新データ106件の両方で全165コマンドが合格。HTML・JS・CSS・JSONスキーマ・アプリ表示バージョンは変更しない。

## HTML肥大化（共有機能開発時の記録）

- 変更前: 28,885 bytes
- 変更後: 28,995 bytes
- 差分: +110 bytes
- 判断: 振る舞いは外部JavaScript、表示は外部CSSへ分離し、大規模なインライン実装は追加していない。
